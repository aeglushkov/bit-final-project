# Async reply from Diwei Su — 2026-04-26

- **With:** Diwei Su
- **Date:** 2026-04-26
- **Format:** Async / written reply (not a live meeting)
- **Topic:** Feedback on the 5-paper reconstruction-agents short list; Su's
  proposal to extend Embodied VideoAgent with brain-like contextual / scene-
  level memory
- **Related:** [notes/survey/reconstruction-agents-tier1-v1-working.md](../survey/reconstruction-agents-tier1-v1-working.md),
  [notes/meetings/2026-04-17.md](2026-04-17.md)
- **Attachment sent to Su:** `reconstruction-agents-v1.pdf` (Tier 1 short list)

## My message

> 苏迪威好！I tried to find papers in the direction of agent frameworks that
> detects gaps in the long video input and calls generative or specialist
> sub-agents to reconstruct the missing content (kind of detect → reconstruct
> → refine loop).
>
> I picked the 5 papers that seemed to fit this criteria — the PDF is
> attached.
>
> These papers are mostly about spatial reasoning on short scenes, where the
> missing information is geometric (off-screen object positions, unseen
> viewpoints, occluded geometry). To be honest, I'm a bit stuck and not sure
> I'm heading in the right direction. Maybe it's because I don't have a full
> understanding of our research.
>
> Also, are these papers in line with what you meant by "reconstruction of
> missing information"?
>
> I'd really appreciate your guidance. Thank you!
>
> [attached reconstruction-agents-v1.pdf]

## Transcript (Diwei Su)

> Hi Sasha! Thank you for sorting out these references. These five papers are
> highly relevant to our research direction. Among them, Embodied VideoAgent
> perfectly aligns with our core ideas. This work constructs a form of working
> memory analogous to that of the human brain, storing observed objects from
> video frames into a dedicated memory bank and retrieving relevant content
> when needed. Even so, the model is limited to perceiving only immediate
> input information and cannot go beyond current visual observations. I
> believe whether an agent can fully leverage long-term memory acquired during
> continual learning will be the key direction for future agent development.
>
> From my perspective, multimodal understanding models capture semantic
> knowledge about the real world, while visual generation models bridge
> abstract concepts and concrete visual content. Therefore, building
> brain-like contextual memory on top of Embodied VideoAgent to fully unleash
> the model's learned knowledge will be critical to our research. Overall,
> your current literature review direction is correct, and you have done an
> excellent job so far.
>
> I also notice a clear limitation of Embodied VideoAgent: it only sparsely
> records isolated objects and individual actions, yet fails to form targeted
> memory for holistic scenes, which explains its unsatisfactory performance
> on tasks like OpenEQA. In contrast, humans excel at associative memory,
> where a single scene can trigger the recall of all connected contextual
> information.
>
> We can take advantage of the collaboration between understanding and
> generation models to expand the sparse object-level memory of Embodied
> VideoAgent into comprehensive scene-level memory. For instance, we can
> associate all items in a given scene with representative scene frames and
> establish a structured memory mapping from scene images to corresponding
> objects. When addressing a scenario-based task, we can sample key frames
> from the video input and feed them into a generation model to reconstruct
> analogous scene layouts, which are then used for cross-matching and
> retrieval within the memory bank.
>
> Take the question "I am sitting on the sofa in the living room and watching
> TV. Which room is behind me?" as an example. The agent can first combine
> stored scene representations from its working memory with the textual query
> as conditional inputs for the generation model to complete contextual
> recollection. The refined, generated scene features will further support
> precise memory retrieval and reasoning, allowing the model to derive the
> correct answer effectively.
>
> I believe that the construction of contextual memory can truly endow agents
> with the core ability to effectively accumulate and reuse acquired world
> knowledge.

## Key points

- **Direction confirmed.** The 5-paper short list is on track. Su explicitly
  says my literature review direction is correct.
- **Embodied VideoAgent is the anchor.** Of the five, Su singles it out as
  perfectly aligned with our core ideas — its object-level working-memory
  bank is the closest existing analogue to what we want to build.
- **Stated limitation of Embodied VideoAgent (Su's framing):**
  - Perceives only the *immediate* input — cannot go beyond current visual
    observations.
  - Memory is *sparse and object-/action-level* — no holistic scene memory.
  - This is why it underperforms on OpenEQA-style scenario questions.
- **Humans = associative memory.** A single scene cue triggers recall of all
  connected context. Embodied VideoAgent's per-object indexing cannot do
  this.
- **Two-model decomposition Su keeps using:**
  - *Multimodal understanding models* → carry semantic knowledge of the world.
  - *Visual generation models* → bridge abstract concepts ↔ concrete visual
    content.
  - Both are needed; the agent's job is to orchestrate them.

## Su's proposal — scene-level contextual memory on top of Embodied VideoAgent

- **Memory structure:** mapping from *representative scene frames* → all
  items / objects in that scene (extension of the current per-object index
  upward to the scene level).
- **At query time:**
  1. Sample key frames from the input video.
  2. Feed key frames + the textual query into a *generation model* as
     conditional inputs to "reconstruct analogous scene layouts" — i.e.
     contextual recollection.
  3. Use the *generated* scene features to cross-match and retrieve from the
     memory bank.
  4. Reason over the retrieved scene + objects to answer.
- **Worked example (Su's):** "I am sitting on the sofa in the living room
  and watching TV. Which room is behind me?"
  - Stored scene reps + query → generation model fills in the recalled
    layout (living room with sofa + TV, plus what's behind the viewpoint).
  - Generated scene features → retrieval + reasoning → answer.
- **Framing claim:** contextual memory is the *core ability* that lets an
  agent accumulate and reuse acquired world knowledge — i.e. this is
  positioned as the central research bet, not a side feature.

## How this connects to earlier discussions

- Builds directly on the "memory mechanisms are key" thread from the
  2026-04-17 reply (hippocampus / efficient memory as the next direction).
- The "detect → reconstruct → refine" loop I was searching for shows up
  here as: *retrieve sparse memory → generate a plausible full scene →
  refine retrieval / reasoning against it*. The reconstruction is from
  memory, not just from the current visual stream.
- The "agent calls a generation model when information is missing" pattern
  from 2026-04-17 is reinforced — but the missing information is now framed
  as *missing context*, not just missing geometry.

## My open questions / things to think about

- What does "comprehensive scene-level memory" look like as a concrete data
  structure? Embeddings of representative frames keyed to object lists? A
  graph over scenes? A learned scene token?
- What generation model is realistic here? A scene-layout / 3D-scene
  generator, or a 2D image generator conditioned on scene text + frame?
- How is "key frame sampling" decided — same machinery as LongVideoAgent's
  *when to look*, or a separate scene-boundary detector?
- Is the OpenEQA gap on Embodied VideoAgent actually attributable to the
  object-vs-scene memory split, or to something else (frame budget,
  retrieval quality)? Worth verifying before designing on top of that
  premise.
- Does this proposal change the Tier 1 short list, or is it purely an
  extension of Embodied VideoAgent?

## Next steps

- [ ] Re-read Embodied VideoAgent with focus on the memory-bank schema and
      OpenEQA failure cases — confirm Su's diagnosis of the object-only
      limitation.
- [ ] Sketch a candidate scene-level memory schema (frame rep → object list,
      or a richer structure) and what the retrieval API would look like.
- [ ] Identify which generation model could plausibly do "reconstruct
      analogous scene layouts" given key frames + text — short list of
      candidates from the broader survey.
- [ ] Re-scan the other 4 papers in the Tier 1 short list for any
      ingredients that fit the scene-level-memory plan (especially anything
      doing scene-level indexing or scene-conditioned generation).
- [ ] Reply to Su with the schema sketch + which generation model I'd reach
      for, before committing to an implementation path.
